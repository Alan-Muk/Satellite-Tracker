interface Props {
  noradId: number;
}

export default function SelectedOrbitPrediction(_props: Props) {
  //
  // Full orbit predictions are rendered by
  // the satellite trail system.
  //
  // Selection only controls the satellite
  // marker and telemetry panel.
  //
  return null;
}
